-- NB: Using global scope to let IntelliSense to do its magic

-- Colors used in the design
Colors = {
  Plugin = { 102, 102, 102 },
}

-- The pages used within the plugin
PageNames = { "Control", "Setup" }
